The ``1-my_list.py`` module
======================

Using ``MyClass``
-------------------

This is an example text file in reStructuredText format.  First import
``factorial`` from the ``example`` module:

    >>> from python-inheritance/1-my_list.py import MyClass

Now use it:

    >>> my_list = MyClass()
    >>> my_list.append(1)
    1

